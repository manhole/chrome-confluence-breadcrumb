{
  "name": "chrome-confluence-breadcrumb",
  "private": true,
  "description": "Chrome extension that displays the page hierarchy (breadcrumb) at the top of Confluence Cloud pages.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "esbuild": "^0.28.1",
    "typescript": "^7.0.2"
  }
}
